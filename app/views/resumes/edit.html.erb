<h1>Editing resume <%= "for #{@resume.get_username}" if @resume.user != current_user %></h1>

<%= render 'form' %>

<%= link_to 'Show', @resume %> |
<%= link_to "Back", nil, onclick: "history.back();" %> |
<%= link_to 'Destroy', @resume, method: :delete, data: { confirm: 'Are you sure?' } %>
